generator client {
  provider = "prisma-client-js"
  output   = "./database"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users {
  id       String @id
  username String @unique
}
